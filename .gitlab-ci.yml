image: node:16.14 #define image for docker

stages:
  - test
  - build
  - deploy

cache:
  paths:
    - node_modules/

test project:
  stage: test
  script:
    - npm install
    - npm test

build project:
  stage: build
  script:
    - npm install
    - CI=false npm run build #if CI=true(default) warning are treated as errors
    - echo "Build successful"
  artifacts:
    paths:
      - build/

deploy project: # here we need exact server
  stage: deploy
  script:
    - echo "Deploying application..."
    - node --version
    - echo "Using credential - $USERNAME, $PASSWORD"
    - echo "Application deployed successfully"
  only:
    - trunk